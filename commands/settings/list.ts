import { Command } from "../../command";

const command: Command = {
  name: "list",
  description: "Lists all class codes from your subscriptions.",
  async execute() {
    return `This feature is not currently available.`;
  },
};

module.exports = command;
